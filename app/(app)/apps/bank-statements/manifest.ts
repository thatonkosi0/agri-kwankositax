import { AppManifest } from "../common"

export const manifest: AppManifest = {
  name: "Bank Statement Analysis",
  description: "Upload a bank statement and let AI extract every line into reviewable transactions",
  icon: "🏦",
}
